spring:
  application:
    name: eureka-server

server:
  port: 8000

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://175.178.16.179:8000/eureka # 注册中心地址